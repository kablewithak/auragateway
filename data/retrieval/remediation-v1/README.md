# Retrieval metadata remediation v1

This directory contains the typed source metadata overlay used by the remediated development
candidates.

The overlay is separate from the frozen corpus and chunk artifacts. It is bound to the frozen corpus
manifest and must cover exactly 30 source IDs.

```text
source_metadata.json
```

The metadata categories are language, interface kind, OAuth grant, and representation kind.
