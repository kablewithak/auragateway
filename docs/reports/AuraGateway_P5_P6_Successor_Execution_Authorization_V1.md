# AuraGateway P5/P6 Successor Execution Authorization V1

## Purpose

Provide a repository-defined, transient, single-use authorization boundary for
one future P5/P6 Successor Runtime Qualification V1 execution.

## Exact predecessor authority

The issuer binds the merged successor implementation carried by main commit:

`6e424acb27e568bb7ce5000ea0732e175bf6b35a`

The bound notebook identity is:

`113197f104f36fd11a9471e46c5a5bb1de939a5669373250694b11359f405fb8`

The bound runtime-script identity is:

`5d6b5594cfb85f5ec52c4e4a7db43f029dc18f2aeadc38648f1d7c4b4c422737`

The bound wrapper-code identity is:

`f65b8dba855fd503b415ccffa78dd3039fe4fdcc4145b077edc6fc4cb16747dd`

## Authorization controls

The static issuer enforces:

- clean synchronized `main`;
- merged-successor ancestry;
- exact predecessor artifact identities;
- fresh Kaggle T4 x2 observation;
- Internet disabled;
- exact input-attachment counts;
- explicit worker GPU mapping;
- single-use authorization;
- no hidden retry;
- no replacement worker;
- one saved version maximum;
- five model requests maximum;
- zero benchmark trajectories;
- zero external network requests;
- zero external spend;
- no customer data or credentials;
- non-overwriting authorization and terminal receipts;
- optional pre-execution abandonment;
- terminal non-reuse for unknown outcomes.

## Runtime boundary

The static implementation does not issue a live authorization.

After merge, the next permitted transition is:

`observe Kaggle -> create canonical operator confirmation -> issue once`

Only a verified live authorization may permit the single governed successor
runtime execution.

## Non-claims

This implementation does not establish:

- current-line P5 cache reuse;
- current-line P5 full-process reset;
- current-line P6 route isolation;
- current-line P6 metric isolation;
- measured A/B/C eligibility;
- deployment readiness;
- production readiness.
